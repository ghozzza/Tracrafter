import React from "react";
import BorrowPage from "./_components/borrow";
import { BorrowBalance } from "@/components/user-balance/borrow-balance";

const Borrow = () => {
  return (
    <div>
      <BorrowPage />
    </div>
  );
};

export default Borrow;
