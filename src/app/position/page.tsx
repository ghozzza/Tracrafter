"use client"
import React from 'react';
import CreatePosition from '../swap/createPosition';

const Position = () => {
    return (
        <div className='flex justify-center items-center mt-16 mx-0'>
            <CreatePosition />
        </div>
    );
};

export default Position;
